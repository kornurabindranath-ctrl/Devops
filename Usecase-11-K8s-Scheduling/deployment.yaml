apiVersion: apps/v1
kind: Deployment
metadata:
  name: normal-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: normal-app
  template:
    metadata:
      labels:
        app: normal-app
    spec:
      containers:
      - name: nginx
        image: nginx
