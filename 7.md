# Day 8 - Cluster Autoscaler versus Karpenter


# Phase 1 - EKS cluster setup

   bash
   ```
   apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: autoscaling-lab
  region: us-east-1
  version: "1.33"

iam:
  withOIDC: true

managedNodeGroups:

  - name: workers

    instanceType: t3.medium

    desiredCapacity: 2

    minSize: 2

    maxSize: 10

    volumeSize: 50

    labels:
      role: worker
   ```

   deploy them

   bash
   ```
   eksctl create cluster -f cluster.yaml
   ```

   wait for 20 mins then verify the worker nodes

   bash
   ```
   kubectl get nodes
   ```
<img width="2768" height="316" alt="image" src="https://github.com/user-attachments/assets/dddbf740-e7b4-4dfe-a984-3569e976f9ce" />

# Phase 2 Install metric server

bash
```
kubectl apply -f \
https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml
```
 verify 

 <img width="2604" height="252" alt="image" src="https://github.com/user-attachments/assets/5673eacc-a4f1-41c4-b1f0-0481968a13f7" />


# Phase 3 Cluster autoscaler scalimg

  Describe the cluster and associate OIDC  to it

  bash
  ```
  aws eks describe-cluster \
--name autoscaling-lab

eksctl utils associate-iam-oidc-provider \
--cluster autoscaling-lab \
--approve
  ```

Create a IAM policy

bash
```
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "autoscaling:SetDesiredCapacity",
        "autoscaling:TerminateInstanceInAutoScalingGroup"
      ],
      "Resource": "*",
      "Condition": {
        "StringEquals": {
          "aws:ResourceTag/k8s.io/cluster-autoscaler/enabled": "true",
          "aws:ResourceTag/k8s.io/cluster-autoscaler/autoscaling-lab": "owned"
        }
      }
    },
    {
      "Effect": "Allow",
      "Action": [
        "autoscaling:DescribeAutoScalingGroups",
        "autoscaling:DescribeAutoScalingInstances",
        "autoscaling:DescribeLaunchConfigurations",
        "autoscaling:DescribeScalingActivities",
        "autoscaling:DescribeTags",
        "ec2:DescribeImages",
        "ec2:DescribeInstanceTypes",
        "ec2:DescribeLaunchTemplateVersions",
        "ec2:GetInstanceTypesFromInstanceRequirements",
        "eks:DescribeNodegroup"
      ],
      "Resource": "*"
    }
  ]
}
```

create it 

bash
```
aws iam create-policy \
--policy-name ClusterAutoscalerPolicy \
--policy-document file://policy.json
```

<img width="2798" height="872" alt="image" src="https://github.com/user-attachments/assets/c52f6691-d634-4234-ae03-788999f23930" />

create a service account

bash
```
eksctl create iamserviceaccount \
--cluster autoscaling-lab \
--namespace kube-system \
--name cluster-autoscaler \
--attach-policy-arn arn:aws:iam::<ACCOUNT_ID>:policy/ClusterAutoscalerPolicy \
--approve
```

<img width="2940" height="1042" alt="image" src="https://github.com/user-attachments/assets/f3b00a9f-9e2a-4e28-9187-049619faf193" />

Install Cluster autoscaler


bash
```
helm repo add autoscaler \
https://kubernetes.github.io/autoscaler

helm repo update
```
 bash
 ```
 helm install cluster-autoscaler \
autoscaler/cluster-autoscaler \
-n kube-system \
--set autoDiscovery.clusterName=autoscaling-lab \
--set awsRegion=us-east-1 \
--set rbac.serviceAccount.create=false \
--set rbac.serviceAccount.name=cluster-autoscaler
 ```
 <img width="2938" height="988" alt="image" src="https://github.com/user-attachments/assets/b66de791-a0fd-40d4-be45-9c4925591fbd" />

 verfiy

 bash
 ```
 kubectl get pods -n kube-system
 ```

 <img width="2920" height="590" alt="image" src="https://github.com/user-attachments/assets/87c1cc96-b0c3-4f11-916a-a95121b61c57" />

  Testing  CA

  bash
  ```
  apiVersion: apps/v1

kind: Deployment

metadata:
  name: inflate

spec:
  replicas: 50

  selector:
    matchLabels:
      app: inflate

  template:
    metadata:
      labels:
        app: inflate

    spec:
      containers:

      - name: pause

        image: public.ecr.aws/eks-distro/kubernetes/pause:3.7

        resources:
          requests:
            cpu: 1
            memory: 1Gi

   ```  

deploy then observe scaling using CA

<img width="2690" height="1030" alt="image" src="https://github.com/user-attachments/assets/3bccdbc3-83a2-4679-aff3-7b60936a953f" />

<img width="2866" height="656" alt="image" src="https://github.com/user-attachments/assets/ddf4a0b9-2b76-4219-8bec-fb7d0d66708a" />


<img width="2940" height="618" alt="image" src="https://github.com/user-attachments/assets/6ff791fe-2cb1-4d60-9e14-7b6ce5dae8e1" />


scale down the deployment

bash
```
kubectl scale deployment inflate \
--replicas=2
```



 Events of scale down

 <img width="2940" height="1162" alt="image" src="https://github.com/user-attachments/assets/b87128b0-6b6a-42d8-88c8-eed9312639fd" />
 
observe the scale down opf the nodes after 15 minutes

<img width="2276" height="946" alt="image" src="https://github.com/user-attachments/assets/2d759363-90c0-4281-82f9-6f806c70413d" />

remove CA and deployment after demonstration

bash
```
helm uninstall cluster-autoscaler \
-n kube-system

 kubectl delete deployment inflate
```



# Phase 4 Karpenter autoscaling


 Tag the subnets and security groups for Karpenter 

 
<img width="2936" height="748" alt="image" src="https://github.com/user-attachments/assets/a0357db6-5a2e-4553-8d63-1758f5367663" />


bash
 ```
 aws ec2 create-tags \
--resources \
subnet-03f3cbc7ccd408ec5 \
subnet-032905feef4a050e3 \
subnet-0dcdda83019192230 \
subnet-0bee2349ecf1668f7 \
--tags Key=karpenter.sh/discovery,Value=autoscaling-lab
 ```

 bash
 ```
 aws ec2 create-tags \
--resources sg-05303e3d17642a0a7 \
--tags Key=karpenter.sh/discovery,Value=autoscaling-lab
```

verfiy them labels because karpenter scales nodes using labels

<img width="2930" height="770" alt="image" src="https://github.com/user-attachments/assets/9dae2df2-7a2f-4756-a773-13fb6432beca" />


Install compatabile Karpeneter version to EKS cluster

bash

```
export KARPENTER_VERSION=1.4.1
```

download Karpenter cloud formation template

bash

```
curl -fsSL \
https://raw.githubusercontent.com/aws/karpenter-provider-aws/v1.4.1/website/content/en/preview/getting-started/getting-started-with-karpenter/cloudformation.yaml \
> karpenter.yaml
```

exporting all variables

bash
```
export CLUSTER_NAME=autoscaling-lab
export AWS_ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
export REGION=us-east-1

echo $CLUSTER_NAME
echo $AWS_ACCOUNT_ID
echo $REGION
```

Now install karpenter using downloaded cloudformation template

bash
```
aws cloudformation deploy \
--stack-name Karpenter \
--template-file karpenter.yaml \
--capabilities CAPABILITY_NAMED_IAM \
--parameter-overrides \
ClusterName=$CLUSTER_NAME
```

<img width="2820" height="530" alt="image" src="https://github.com/user-attachments/assets/230773ba-3a4a-4b5c-976a-d9dc31746abf" />

verify the stack

<img width="2848" height="298" alt="image" src="https://github.com/user-attachments/assets/8ae75809-bd10-485d-a008-412d8cb96ee5" />

verify SQS queue

bash
```
aws sqs list-queues
```

Installing karpenter controller

Install Helm chart

Add OCI repo access:

bash
```
helm registry login public.ecr.aws
```

Install Karpenter:

bash
```
helm upgrade --install karpenter \
oci://public.ecr.aws/karpenter/karpenter \
--namespace kube-system \
--create-namespace \
--version ${KARPENTER_VERSION} \
--set serviceAccount.annotations."eks\.amazonaws\.com/role-arn"="arn:aws:iam::${AWS_ACCOUNT_ID}:role/KarpenterControllerRole-${CLUSTER_NAME}" \
--set settings.clusterName=${CLUSTER_NAME} \
--set settings.interruptionQueue=${CLUSTER_NAME}
```
<img width="2932" height="946" alt="image" src="https://github.com/user-attachments/assets/0e05bbfe-4f56-4cfb-90b2-1d2adb691769" />

verify ther installation

<img width="2886" height="634" alt="image" src="https://github.com/user-attachments/assets/478eb428-103e-4113-a048-4ac7a46e6c7a" />

Got the issue with Role attached to service account trust policy is wrong where as OIDC provider of the cluster is wrong

checking OIDC provider

bash
```
export OIDC_PROVIDER=$(aws eks describe-cluster \
--name autoscaling-lab \
--query "cluster.identity.oidc.issuer" \
--output text | sed 's|https://||')

echo $OIDC_PROVIDER

```
update the role trust relationship

bash
```
aws iam update-assume-role-policy \
--role-name KarpenterControllerRole-autoscaling-lab \
--policy-document file://trust-policy.json
```

restart karpenter

bash
```
kubectl rollout restart deployment karpenter \
-n kube-system
```

Verify the pods

<img width="2898" height="634" alt="image" src="https://github.com/user-attachments/assets/376e2ecb-3df3-4300-874f-9d7af0ea796d" />

Verify CRDS

<img width="2900" height="270" alt="image" src="https://github.com/user-attachments/assets/daa85cfb-61b3-4623-8545-8ad9be8ad0ad" />

Creating EC2NodeClass

bash
```
apiVersion: karpenter.k8s.aws/v1
kind: EC2NodeClass

metadata:
  name: default

spec:

  amiFamily: AL2023

  role: KarpenterNodeRole-autoscaling-lab

  subnetSelectorTerms:
    - tags:
        karpenter.sh/discovery: autoscaling-lab

  securityGroupSelectorTerms:
    - tags:
        karpenter.sh/discovery: autoscaling-lab

  tags:
    karpenter.sh/discovery: autoscaling-lab
```

deploy and verify

bash
```
kubectl describe ec2nodeclass
```

<img width="2644" height="204" alt="image" src="https://github.com/user-attachments/assets/a66de8ef-fff5-49bc-8639-1c4890c42204" />


creating NodePool

bash
```
apiVersion: karpenter.sh/v1
kind: NodePool

metadata:
  name: default

spec:
  template:
    spec:

      nodeClassRef:
        group: karpenter.k8s.aws
        kind: EC2NodeClass
        name: default

      requirements:

        - key: kubernetes.io/arch
          operator: In
          values:
            - amd64

        - key: kubernetes.io/os
          operator: In
          values:
            - linux

        - key: karpenter.sh/capacity-type
          operator: In
          values:
            - on-demand

        - key: karpenter.k8s.aws/instance-category
          operator: In
          values:
            - t

        - key: karpenter.k8s.aws/instance-generation
          operator: Gt
          values:
            - "2"

  disruption:
    consolidationPolicy: WhenEmptyOrUnderutilized
    consolidateAfter: 30s

  limits:
    cpu: "100"

  weight: 10
  ```

  deploy and verify

  <img width="2802" height="304" alt="image" src="https://github.com/user-attachments/assets/88ee9600-a664-402b-9d6c-ba84196dc2b1" />


  # Provisioning benchmark

  Reproducing same workload as CA but creating inflate

  bash
  ```
  apiVersion: apps/v1
kind: Deployment

metadata:
  name: inflate

spec:
  replicas: 30

  selector:
    matchLabels:
      app: inflate

  template:
    metadata:
      labels:
        app: inflate

    spec:
      terminationGracePeriodSeconds: 0

      containers:
      - name: pause
        image: public.ecr.aws/eks-distro/kubernetes/pause:3.7

        resources:
          requests:
            cpu: "1000m"
            memory: "1Gi"
  ```

  deploy nad verify the scaling using karpenter

  now got another issue  karpeneter is not associated with the role

  <img width="2936" height="1510" alt="image" src="https://github.com/user-attachments/assets/95dbd3b0-a794-4147-b7fb-0dd93e3a523f" />

  edit the configmap used 

  bash
  ```
  kubectl edit configmap aws-auth -n kube-system
  ```

 then Node claims

  <img width="2516" height="480" alt="image" src="https://github.com/user-attachments/assets/fec2d2c5-a39c-4290-b1cb-c8ead6d0ea52" />
  
  Nodes are created
  
  <img width="2248" height="808" alt="image" src="https://github.com/user-attachments/assets/9bd98bdc-eddb-42b1-a45d-423ab9402396" />

  
  total 30 replicas Pods are scheduled
  
 <img width="2392" height="1374" alt="image" src="https://github.com/user-attachments/assets/7f9b1d74-f9d9-4653-a228-830b7e4aa9c7" />

Scale down the deployment

bash
```
 kubectl scale deployment inflate \
--replicas=2
```

then we can see node count will decrease

<img width="1928" height="372" alt="image" src="https://github.com/user-attachments/assets/cfd31431-2a2d-42e8-b21e-fe62e9909263" />


  
 



  




















