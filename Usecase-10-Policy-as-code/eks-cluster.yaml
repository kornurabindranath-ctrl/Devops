apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: opa-gatekeeper-lab
  region: us-east-1
  version: "1.33"

managedNodeGroups:

  - name: workers

    instanceType: t3.medium

    desiredCapacity: 2

    minSize: 2

    maxSize: 3

    volumeSize: 20
